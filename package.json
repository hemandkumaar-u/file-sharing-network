{
  "name": "qr-file-drop",
  "version": "1.0.0",
  "description": "Local Network QR File Drop",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "localtunnel": "^2.0.2",
    "multer": "^1.4.5-lts.1",
    "qrcode-terminal": "^0.12.0",
    "untun": "^0.2.2"
  }
}
